<% provide(:title, @user.name) %>

<div class="row">
  <aside class="span4">
  	<%= render 'shared/flashes' %>
    <section>
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>


  <div class="center hero-unit">
  <% @wants.each do |item| %>
    <p><% my_wants = Item.find(item.item_id) %><p>
     <p><%= my_wants.content %><p>
  <% end %>
</div>

<p>Find me in app/views/users/show.html.erb</p>

